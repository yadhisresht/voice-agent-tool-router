# voice-agent-tool-router
A Python-based intelligent voice assistant backend that uses embeddings, cosine similarity, learning-to-rank (LTR), and rule-based NER to select and execute automotive tools from natural language queries.
